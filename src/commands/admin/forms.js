const {SlashCommandBuilder,ActionRowBuilder,StringSelectMenuBuilder,ModalBuilder,TextInputBuilder,TextInputStyle,PermissionFlagsBits}=require("discord.js");
const config=require("../../config/config"); const {embed}=require("../../utils");
module.exports={data:new SlashCommandBuilder().setName("forms").setDescription("Manage APEX forms.").setDefaultMemberPermissions(PermissionFlagsBits.ManageGuild).addSubcommand(s=>s.setName("panel").setDescription("Post the forms panel.")),async execute(i){const menu=new StringSelectMenuBuilder().setCustomId("forms_select").setPlaceholder("Choose a form").addOptions({label:"Staff Application",value:"staff",emoji:"👮"},{label:"Player Report",value:"report",emoji:"🚨"},{label:"Ban Appeal",value:"appeal",emoji:"⚖️"},{label:"Business Application",value:"business",emoji:"🏢"});await i.channel.send({embeds:[embed("📋 APEX Forms","Select a form below. Every submission is stored and sent to the configured logs channel.")],components:[new ActionRowBuilder().addComponents(menu)]});await i.reply({content:"✅ Forms panel posted.",ephemeral:true});}};
